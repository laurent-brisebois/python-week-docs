# Git

```shell
# clone a repo in your current directory
git clone <repo>

# checkout a specific branch
git checkout master

# pull the latest changes
git pull

# commit and push your new code
git stage script.py
git commit -m "add script.py"
git push
```